<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/blue10</item>
        <item name="colorPrimaryDark">@color/blue06</item>
        <item name="colorAccent">@color/primary_yellow</item>
        <item name="android:toolbarStyle">@style/Toolbar</item>
    </style>

    <style name="PagerTabStripText">
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <style name="Toolbar" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@color/blue10</item>
        <item name="android:popupTheme">@style/Theme.AppCompat.Light</item>

    </style>

</resources>
